# C++ 项目完整开发指南

## 项目概述

这是一个使用 **CMake + MinGW + GDB** 的 C++ 项目配置及调试完整指南。

---

## 目录结构

```
Features/
├── .vscode/                          # VS Code 配置文件夹
│   ├── launch.json                   # 调试配置
│   ├── tasks.json                    # 编译任务配置
│   └── c_cpp_properties.json         # C++ IntelliSense 配置
├── src/                              # 源文件目录
│   └── main.cpp                      # 主程序文件
├── include/                          # 头文件目录（可选）
├── build/                            # 构建输出目录
│   └── Features.exe                  # 编译生成的可执行文件
├── CMakeLists.txt                    # CMake 配置文件
├── .gitignore                        # Git 忽略配置
└── README.md                         # 项目说明
```

---

## 第一部分：项目创建

### 1. 创建项目目录结构

```powershell
# 创建主项目目录
mkdir e:\CodeProject\Features
cd e:\CodeProject\Features

# 创建子目录
mkdir src
mkdir include
mkdir build
mkdir .vscode
```

### 2. 项目文件说明

#### 2.1 CMakeLists.txt（构建配置）
```cmake
cmake_minimum_required(VERSION 3.10)
project(Features)

set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

# 设置调试信息
if(NOT CMAKE_BUILD_TYPE)
    set(CMAKE_BUILD_TYPE Debug)
endif()
set(CMAKE_CXX_FLAGS_DEBUG "-g3 -O0")
set(CMAKE_C_FLAGS_DEBUG "-g3 -O0")

# 添加源文件
set(SOURCES
    src/main.cpp
)

# 包含目录
include_directories(include)

# 创建可执行文件
add_executable(Features ${SOURCES})

# 编译选项
if(MSVC)
    target_compile_options(Features PRIVATE /W4)
else()
    target_compile_options(Features PRIVATE -Wall -Wextra -Wpedantic)
endif()
```

**功能说明：**
- 指定 C++ 标准为 C++17
- 启用调试符号（-g3）和关闭优化（-O0）便于调试
- 添加编译警告选项

#### 2.2 src/main.cpp（主程序）
```cpp
#include <iostream>

int main() {
    std::cout << "Hello, Features Project!" << std::endl;
    return 0;
}
```

#### 2.3 .vscode/launch.json（调试配置）
```json
{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "C++ Debug (GDB)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/Features.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "MIMode": "gdb",
            "miDebuggerPath": "gdb.exe",
            "setupCommands": [
                {
                    "description": "Enable pretty-printing for gdb",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                }
            ],
            "preLaunchTask": "build",
            "sourceLanguages": ["cpp"],
            "logging": {
                "trace": true,
                "traceResponse": true,
                "engineLogging": true
            }
        }
    ]
}
```

**功能说明：**
- 配置 GDB 调试器
- 指定可执行文件路径
- 设置调试前自动编译
- 启用调试日志

#### 2.4 .vscode/tasks.json（编译任务）
```json
{
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "cmake",
            "args": [
                "--build",
                "${workspaceFolder}/build",
                "--config",
                "Debug"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": "$gcc",
            "presentation": {
                "echo": true,
                "reveal": "always",
                "focus": false,
                "panel": "shared"
            }
        },
        {
            "type": "cppbuild",
            "label": "C/C++: g++.exe build active file",
            "command": "D:\\SoftWare\\mingw64\\bin\\g++.exe",
            "args": [
                "-fdiagnostics-color=always",
                "-g",
                "${file}",
                "-o",
                "${fileDirname}\\${fileBasenameNoExtension}.exe"
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": ["$gcc"],
            "group": {"kind": "build"},
            "detail": "Task generated by Debugger."
        }
    ],
    "version": "2.0.0"
}
```

**功能说明：**
- 定义 "build" 任务使用 CMake 编译
- 定义备用任务使用 g++ 直接编译单个文件

#### 2.5 .vscode/c_cpp_properties.json（IntelliSense 配置）
```json
{
    "configurations": [
        {
            "name": "Win32",
            "includePath": [
                "${workspaceFolder}/**",
                "D:/SoftWare/mingw64/lib/gcc/x86_64-w64-mingw32/8.1.0/include/c++",
                "D:/SoftWare/mingw64/lib/gcc/x86_64-w64-mingw32/8.1.0/include/c++/x86_64-w64-mingw32",
                "D:/SoftWare/mingw64/lib/gcc/x86_64-w64-mingw32/8.1.0/include",
                "D:/SoftWare/mingw64/x86_64-w64-mingw32/include"
            ],
            "defines": ["_DEBUG", "UNICODE", "_UNICODE"],
            "compilerPath": "D:/SoftWare/mingw64/bin/g++.exe",
            "cStandard": "c++17",
            "cppStandard": "c++17",
            "intelliSenseMode": "gcc-x64",
            "browse": {
                "path": [
                    "${workspaceFolder}",
                    "D:/SoftWare/mingw64"
                ],
                "limitSymbolsToIncludedHeaders": true
            }
        }
    ],
    "version": 4
}
```

**功能说明：**
- 配置 VS Code C++ 代码提示
- 指定 MinGW 编译器路径
- 设置 C++ 标准库搜索路径

---

## 第二部分：编译步骤

### 方法 1：首次编译（完整步骤）

```powershell
# 进入项目根目录
cd e:\CodeProject\Features

# 进入 build 目录
cd build

# 使用 CMake 配置 MinGW 编译
cmake -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Debug ..

# 编译项目
cmake --build .
```

**输出示例：**
```
-- The C compiler identification is GNU 8.1.0
-- The CXX compiler identification is GNU 8.1.0
-- Configuring done (6.1s)
-- Generating done (0.0s)
-- Build files have been written to: E:/CodeProject/Features/build
[ 50%] Building CXX object CMakeFiles/Features.dir/src/main.cpp.obj
[100%] Linking CXX executable Features.exe
[100%] Built target Features
```

### 方法 2：增量编译（修改代码后）

```powershell
cd e:\CodeProject\Features\build
cmake --build .
```

### 方法 3：完全重新编译

```powershell
cd e:\CodeProject\Features

# 清除旧的构建文件
rmdir /s /q build
mkdir build
cd build

# 重新配置和编译
cmake -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Debug ..
cmake --build .
```

---

## 第三部分：运行程序

### 直接运行

```powershell
cd e:\CodeProject\Features\build
.\Features.exe
```

**输出：**
```
Hello, Features Project!
```

### 在 VS Code 中运行

1. 在 VS Code 中打开项目
2. 按 `Ctrl + F5` 快速运行（不调试）
3. 或点击菜单 Run > Run Without Debugging

---

## 第四部分：调试步骤

### 方法 1：使用 VS Code 调试界面（推荐）

**第一步：设置断点**
1. 打开 `src/main.cpp` 文件
2. 在代码行号左侧点击添加断点（红点会出现）
   - 例如：在第 5 行 `std::cout` 行左侧点击

**第二步：启动调试**
- 按 `F5` 启动调试，或
- 点击菜单 Run > Start Debugging，或
- 点击左侧"运行和调试"图标，然后点击"启动调试"按钮

**第三步：调试操作**

| 按键 | 功能 |
|------|------|
| `F5` | 继续执行 |
| `F10` | 单步执行（Step Over） |
| `F11` | 逐句执行（Step Into） |
| `Shift+F11` | 执行完当前函数（Step Out） |
| `Ctrl+F5` | 重新启动调试 |

**第四步：查看信息**
- **变量面板**：左上角显示所有局部变量和全局变量
- **调用堆栈**：显示函数调用关系
- **断点列表**：管理所有设置的断点
- **调试控制台**：输出程序信息和调试命令

### 方法 2：命令行调试（使用 GDB）

```powershell
cd e:\CodeProject\Features\build

# 启动 GDB 调试器
gdb .\Features.exe

# 在 GDB 中输入命令
(gdb) break main          # 在 main 函数设置断点
(gdb) run                 # 运行程序
(gdb) next                # 单步执行
(gdb) print variable_name # 打印变量值
(gdb) continue            # 继续执行
(gdb) quit                # 退出调试
```

---

## 第五部分：常见问题解决

### Q1：编译时找不到 iostream

**解决方案：**
确保 `c_cpp_properties.json` 中的编译器路径正确。更新路径为您的 MinGW 实际安装位置：

```json
"compilerPath": "D:/SoftWare/mingw64/bin/g++.exe"
```

然后重启 VS Code 或执行：Ctrl + Shift + P → "C_Cpp: Reset IntelliSense Database"

### Q2：调试时提示找不到 build 任务

**解决方案：**
1. 确保 `tasks.json` 中有名为 "build" 的任务
2. 确保 build 文件夹存在且包含 CMake 生成的文件
3. 重启 VS Code

### Q3：编译失败

**排查步骤：**
```powershell
# 1. 检查编译器是否可用
g++ --version

# 2. 检查 CMake 是否可用
cmake --version

# 3. 完全重新构建
cd e:\CodeProject\Features
rmdir /s /q build
mkdir build
cd build
cmake -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Debug ..
cmake --build . --verbose
```

### Q4：调试器无法启动

**排查步骤：**
```powershell
# 1. 检查 GDB 是否可用
gdb --version

# 2. 确保可执行文件存在
ls e:\CodeProject\Features\build\Features.exe

# 3. 检查可执行文件是否包含调试符号
gdb .\Features.exe
(gdb) file .\Features.exe
(gdb) quit
```

---

## 第六部分：扩展使用

### 添加新的源文件

1. **在 src/ 目录下创建新文件**，例如 `utils.cpp`

2. **更新 CMakeLists.txt**：
```cmake
set(SOURCES
    src/main.cpp
    src/utils.cpp      # 添加新文件
)
```

3. **重新编译**：
```powershell
cd e:\CodeProject\Features\build
cmake --build .
```

### 使用头文件

1. **在 include/ 目录下创建头文件**，例如 `utils.h`
2. **在源文件中包含**：
```cpp
#include "utils.h"
```
3. include 目录已在 CMakeLists.txt 中指定，不需要额外配置

### 添加第三方库

在 CMakeLists.txt 中添加：
```cmake
# 示例：添加 Boost 库
find_package(Boost REQUIRED)
target_link_libraries(Features ${Boost_LIBRARIES})
```

---

## 第七部分：快速参考命令

### 编译命令
```powershell
# 首次编译
cd build && cmake -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Debug .. && cmake --build .

# 增量编译
cmake --build build

# 清理后重新编译
rmdir /s /q build & mkdir build & cd build & cmake -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Debug .. && cmake --build .
```

### 运行命令
```powershell
# 直接运行
.\build\Features.exe

# 同时编译和运行
cmake --build build && .\build\Features.exe
```

### 调试命令
```powershell
# 使用 VS Code 调试：按 F5

# 使用 GDB 调试
gdb .\build\Features.exe
```

---

## 第八部分：VS Code 快捷键总结

| 快捷键 | 功能 |
|--------|------|
| `Ctrl + Shift + B` | 编译项目 |
| `Ctrl + F5` | 运行程序（不调试） |
| `F5` | 启动调试 |
| `F9` | 添加/移除断点 |
| `F10` | 单步执行 |
| `F11` | 逐句执行 |
| `Shift + F11` | 执行完当前函数 |
| `Ctrl + /` | 注释/取消注释 |
| `Ctrl + S` | 保存文件 |

---

## 第九部分：环境检查清单

在开始项目之前，请确保已安装：

- [ ] MinGW（G++、GDB）
  ```powershell
  g++ --version
  gdb --version
  ```

- [ ] CMake
  ```powershell
  cmake --version
  ```

- [ ] VS Code
  ```powershell
  code --version
  ```

- [ ] VS Code 扩展：
  - [ ] C/C++ (ms-vscode.cpptools)
  - [ ] CMake (twxs.cmake)
  - [ ] CMake Tools (ms-vscode.cmake-tools)

---

## 总结

| 阶段 | 操作 | 快捷键/命令 |
|------|------|-----------|
| **第一次编译** | 进入 build 目录，运行 CMake 配置和编译 | `cmake .. && cmake --build .` |
| **修改代码后编译** | 在 build 目录运行编译 | `cmake --build .` 或 `Ctrl+Shift+B` |
| **运行程序** | 执行可执行文件 | `.\Features.exe` 或 `Ctrl+F5` |
| **调试程序** | 添加断点，启动调试 | `F5` |
| **单步执行** | 逐行执行代码 | `F10`（Step Over）或 `F11`（Step Into） |

---

## 获取帮助

遇到问题时：
1. 查看 VS Code 的"问题"面板（Ctrl+Shift+M）
2. 查看"调试控制台"的错误信息
3. 查看"输出"面板中 CMake 的编译日志
4. 按 F5 重新启动调试

祝您编码愉快！

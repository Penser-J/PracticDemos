# 快速参考卡片

## 项目目录结构速览

```
Features/
├── src/main.cpp          ← 主程序文件
├── build/Features.exe    ← 编译输出
├── .vscode/              ← VS Code 配置
│   ├── launch.json       ← 调试配置
│   ├── tasks.json        ← 编译任务
│   └── c_cpp_properties.json ← 代码提示配置
└── CMakeLists.txt        ← 构建配置
```

---

## 三大操作流程

### 📝 编译流程

```powershell
# 初次编译（完整）
cd e:\CodeProject\Features\build
cmake -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Debug ..
cmake --build .

# 修改代码后编译
cd e:\CodeProject\Features\build
cmake --build .

# 或在 VS Code 中
Ctrl + Shift + B
```

### ▶️ 运行流程

```powershell
# 方式 1：直接运行可执行文件
cd e:\CodeProject\Features\build
.\Features.exe

# 方式 2：VS Code 中不调试运行
Ctrl + F5
```

### 🐛 调试流程

```powershell
# 步骤 1：在代码行号左侧点击添加断点（红点）
# 步骤 2：按 F5 启动调试
# 步骤 3：使用调试快捷键
  F10    - 单步执行
  F11    - 逐句执行
  F5     - 继续执行
  Shift+F11 - 执行完当前函数
```

---

## 常用快捷键

| 快捷键 | 功能 |
|--------|------|
| `Ctrl+Shift+B` | 编译 |
| `Ctrl+F5` | 运行不调试 |
| `F5` | 启动调试 |
| `F9` | 添加/移除断点 |
| `F10` | 单步执行 |
| `F11` | 逐句执行 |
| `Shift+F11` | 函数返回 |

---

## 常用命令（PowerShell）

```powershell
# 进入构建目录
cd e:\CodeProject\Features\build

# 编译
cmake --build .

# 运行
.\Features.exe

# 编译 + 运行
cmake --build . ; .\Features.exe

# 完整重新编译
cd ..
rmdir /s /q build
mkdir build
cd build
cmake -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Debug ..
cmake --build .
```

---

## 问题排查

| 问题 | 解决方案 |
|------|---------|
| 找不到 iostream | Ctrl+Shift+P → Reset IntelliSense |
| 调试时找不到 build | 确保 tasks.json 中有 "build" 任务 |
| 编译失败 | cmake --build . --verbose 查看详细错误 |
| GDB 无法启动 | gdb --version 检查是否安装 |

---

## MinGW 环境检查

```powershell
# 检查编译器
g++ --version

# 检查调试器
gdb --version

# 检查 CMake
cmake --version
```

---

## 编辑器配置位置

| 配置文件 | 用途 | 编辑建议 |
|---------|------|--------|
| `.vscode/launch.json` | 调试配置 | ⚠️ 不推荐手工编辑 |
| `.vscode/tasks.json` | 编译任务 | ⚠️ 不推荐手工编辑 |
| `.vscode/c_cpp_properties.json` | 代码提示 | ✅ 可根据编译器路径调整 |
| `CMakeLists.txt` | 项目构建 | ✅ 添加新文件时需要更新 |

---

## 添加新源文件步骤

1. 在 `src/` 目录创建新文件（如 `utils.cpp`）
2. 编辑 `CMakeLists.txt`：
   ```cmake
   set(SOURCES
       src/main.cpp
       src/utils.cpp    ← 添加这行
   )
   ```
3. 重新编译：`cmake --build build`

---

## 完整工作流示例

```powershell
# 1. 打开项目
cd e:\CodeProject\Features

# 2. 编辑代码（在 VS Code 中） - Ctrl+S 保存

# 3. 编译
cd build
cmake --build .

# 4. 测试运行
.\Features.exe

# 5. 如需调试
# - 添加断点（F9）
# - 按 F5 启动调试
```

---

## 调试窗口布局

```
┌─────────────────────────────────────────────┐
│ VS Code 调试界面布局                           │
├──────────┬──────────────────────────────────┤
│ 左侧面板  │ 编辑器                            │
│ • 变量    │ • 代码                            │
│ • 监察    │ • 断点（红点标记）                 │
│ • 调用栈  │                                  │
│ • 断点    │                                  │
├──────────┴──────────────────────────────────┤
│ 调试控制台（底部）- 查看程序输出              │
└─────────────────────────────────────────────┘
```

---

## 关键文件快速编辑

### 修改 main.cpp 中的代码

```cpp
#include <iostream>

int main() {
    int x = 10;           // ← 添加变量
    int y = 20;           // ← 添加变量
    std::cout << "x + y = " << (x + y) << std::endl;
    return 0;             // ← 在这里设置断点 (F9)
}
```

然后：
1. 按 `Ctrl+S` 保存
2. 按 `Ctrl+Shift+B` 编译
3. 按 `F5` 调试

---

## 调试技巧

### 查看变量值
- 悬停鼠标在变量上查看其当前值
- 或在"变量"面板中查看

### 条件断点
- 右键点击断点 → 编辑断点 → 输入条件表达式

### 调试控制台命令
```
(gdb) print variable_name    # 打印变量值
(gdb) set variable=value     # 修改变量值
(gdb) info locals            # 显示所有局部变量
```

---

## 性能优化编译

```powershell
# 构建类型
-DCMAKE_BUILD_TYPE=Debug    # 调试模式（包含符号）
-DCMAKE_BUILD_TYPE=Release  # 发布模式（优化，无符号）

# 使用 Release 编译
cd build
cmake -DCMAKE_BUILD_TYPE=Release ..
cmake --build .
```

---

**最后修改：2026-02-24**

# 配置检查清单与故障排除

## 一、初始化检查（第一次设置）

### ✅ 系统环境检查

- [ ] **MinGW 已安装**
  ```powershell
  g++ --version
  # 应显示：g++ (x86_64-win32-seh-rev0, Built by MinGW-W64 project) 8.1.0
  ```

- [ ] **GDB 调试器已安装**
  ```powershell
  gdb --version
  # 应显示：GNU gdb (GDB) 8.1
  ```

- [ ] **CMake 已安装**
  ```powershell
  cmake --version
  # 应显示：cmake version 3.x.x 或更高
  ```

- [ ] **VS Code 已安装**
  ```powershell
  code --version
  ```

### ✅ VS Code 扩展检查

打开 VS Code Extensions（Ctrl+Shift+X），确保已安装：

- [ ] **ms-vscode.cpptools** - C/C++
  - 提供调试和代码提示支持

- [ ] **twxs.cmake** - CMake
  - CMake 语法高亮

- [ ] **ms-vscode.cmake-tools** - CMake Tools
  - CMake 构建工具集成

### ✅ 项目文件检查

在 `e:\CodeProject\Features` 目录下，确保存在：

- [ ] `CMakeLists.txt` - 构建配置文件
- [ ] `src/main.cpp` - 主程序文件
- [ ] `include/` - 头文件目录（可为空）
- [ ] `build/` - 构建目录
- [ ] `.vscode/launch.json` - 调试配置
- [ ] `.vscode/tasks.json` - 编译任务
- [ ] `.vscode/c_cpp_properties.json` - 代码提示配置

### ✅ 编译路径检查

验证 MinGW 编译器路径：

```powershell
# 检查 g++ 路径
where g++
# 预期输出：D:\SoftWare\mingw64\bin\g++.exe 或类似路径

# 检查 gdb 路径
where gdb
# 预期输出：D:\SoftWare\mingw64\bin\gdb.exe 或类似路径

# 记录你的编译器路径，用于更新配置文件
```

---

## 二、首次编译检查

### ✅ CMake 配置步骤

```powershell
# 进入项目
cd e:\CodeProject\Features

# 进入 build 目录
cd build

# 运行 CMake 配置
cmake -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Debug ..
```

**预期输出应包含：**
```
-- The C compiler identification is GNU X.X.X
-- The CXX compiler identification is GNU X.X.X
-- Configuring done
-- Build files have been written to: ...
```

**常见问题与解决：**

| 错误信息 | 原因 | 解决 |
|---------|------|------|
| `cmake: command not found` | CMake 未安装或未在 PATH | 安装 CMake 或添加到环境变量 |
| `Could not find generator "MinGW Makefiles"` | MinGW 未正确安装 | 检查 `where g++` 的输出 |
| `Configuring done with warnings` | 编译器配置警告 | 通常可以忽略，继续编译 |

### ✅ 编译步骤

```powershell
# 仍在 build 目录中
cmake --build .
```

**预期输出应包含：**
```
[ 50%] Building CXX object CMakeFiles/Features.dir/src/main.cpp.obj
[100%] Linking CXX executable Features.exe
[100%] Built target Features
```

**常见问题与解决：**

| 错误信息 | 原因 | 解决 |
|---------|------|------|
| `cannot find -lstdc++` | 缺少 C++ 标准库 | 重新安装 MinGW |
| `undefined reference to main` | 源文件不存在或路径错误 | 检查 `src/main.cpp` 是否存在 |
| `permission denied` | 权限问题 | 以管理员身份运行 PowerShell |

### ✅ 编译验证

```powershell
# 检查可执行文件是否生成
ls .\Features.exe

# 运行程序
.\Features.exe
```

**预期输出：**
```
Hello, Features Project!
```

---

## 三、IntelliSense 代码提示故障排除

### 问题：红色下划线显示 "找不到 iostream"

#### 方案 1：重置 IntelliSense 数据库

1. 按 `Ctrl+Shift+P` 打开命令面板
2. 输入 "C_Cpp: Reset IntelliSense Database"
3. 回车执行
4. 重启 VS Code

#### 方案 2：更新编译器路径

1. 打开 `.vscode/c_cpp_properties.json`
2. 更新以下部分为你的 MinGW 实际路径：

```json
{
    "compilerPath": "D:/SoftWare/mingw64/bin/g++.exe",
    "includePath": [
        "D:/SoftWare/mingw64/lib/gcc/x86_64-w64-mingw32/8.1.0/include/c++",
        "D:/SoftWare/mingw64/lib/gcc/x86_64-w64-mingw32/8.1.0/include/c++/x86_64-w64-mingw32",
        "D:/SoftWare/mingw64/lib/gcc/x86_64-w64-mingw32/8.1.0/include",
        "D:/SoftWare/mingw64/x86_64-w64-mingw32/include"
    ]
}
```

#### 方案 3：查找正确的 MinGW 路径

```powershell
# 找到 iostream 路径
where /R D:\SoftWare\mingw64 iostream

# 或手动查找
cd D:\SoftWare\mingw64
ls -r *.h | findstr iostream
```

---

## 四、编译任务故障排除

### 问题：按 Ctrl+Shift+B 编译失败或找不到任务

#### 检查清单

- [ ] `.vscode/tasks.json` 文件存在且包含 "build" 任务
- [ ] 任务定义正确：
  ```json
  {
      "label": "build",
      "type": "shell",
      "command": "cmake",
      ...
  }
  ```
- [ ] `build/` 目录存在
- [ ] `build/` 目录已通过 CMake 初始化

#### 解决步骤

```powershell
# 1. 删除旧任务缓存
rm -r ~/.vscode/workspaceStorage

# 2. 重启 VS Code

# 3. 尝试手动编译验证
cd e:\CodeProject\Features\build
cmake --build .

# 4. 如果手动编译成功，在 VS Code 中重试 Ctrl+Shift+B
```

---

## 五、调试故障排除

### 问题 1：按 F5 后显示"找不到 build"

**原因：** CMake 未生成 Features.exe

**解决：**
```powershell
cd e:\CodeProject\Features\build

# 检查是否已编译
ls Features.exe

# 如果不存在，重新编译
cmake --build .
```

### 问题 2：调试器无法启动

**排查步骤：**

1. **检查 GDB 是否可用**
   ```powershell
   gdb --version
   ```

2. **检查可执行文件是否有调试符号**
   ```powershell
   file .\build\Features.exe
   # 应包含 "debug" 或 "ELF 64-bit"
   ```

3. **手动启动 GDB 测试**
   ```powershell
   cd e:\CodeProject\Features\build
   gdb .\Features.exe
   (gdb) run
   (gdb) quit
   ```

4. **检查 launch.json 配置**
   - 确保 `miDebuggerPath` 是 "gdb.exe"
   - 确保 `program` 路径正确

### 问题 3：断点不能暂停（进程直接运行完成）

**可能原因：** 编译时未包含调试符号

**解决：** 重新编译 Debug 版本
```powershell
cd e:\CodeProject\Features\build
cmake -DCMAKE_BUILD_TYPE=Debug ..
cmake --build . --clean-first
cmake --build .
```

### 问题 4：调试时程序崩溃或异常退出

**排查步骤：**
1. 查看调试控制台输出找线索
2. 检查断点位置的代码逻辑
3. 在关键位置添加更多断点逐步调试
4. 查看"变量"面板中各变量的值

---

## 六、运行问题排除

### 问题 1：运行 exe 显示 "找不到文件"

```powershell
# 确认文件位置
ls e:\CodeProject\Features\build\Features.exe

# 如果不存在，重新编译
cd e:\CodeProject\Features\build
cmake --build .
```

### 问题 2：运行 exe 显示缺少 DLL

**常见缺失 DLL：**
- `libgcc_s_seh-1.dll`
- `libstdc++-6.dll`

**解决方案 1：** 复制 MinGW 运行库
```powershell
# 复制必要的 DLL 到 build 目录
copy D:\SoftWare\mingw64\bin\libgcc_s_seh-1.dll e:\CodeProject\Features\build\
copy D:\SoftWare\mingw64\bin\libstdc++-6.dll e:\CodeProject\Features\build\
```

**解决方案 2：** 静态链接（修改 CMakeLists.txt）
```cmake
# 在 add_executable 前添加
set(CMAKE_EXE_LINKER_FLAGS "-static-libgcc -static-libstdc++ -static")
add_executable(Features ${SOURCES})
```

---

## 七、完全重置与重新安装

### 当以上方案都不工作时

```powershell
# 1. 关闭 VS Code

# 2. 删除所有构建文件
cd e:\CodeProject\Features
rmdir /s /q build
rmdir /s /q .vscode

# 3. 删除 VS Code 缓存
rmdir /s /q ~/.vscode

# 4. 重新创建 .vscode 目录和配置文件（参考完整开发指南）

# 5. 重新打开 VS Code

# 6. 重新编译
cd build
cmake -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Debug ..
cmake --build .
```

---

## 八、配置验收标准

编译、运行、调试都应该满足以下标准才算配置成功：

### ✅ 编译成功标准

```
[ 50%] Building CXX object CMakeFiles/Features.dir/src/main.cpp.obj
[100%] Linking CXX executable Features.exe
[100%] Built target Features
```

### ✅ 运行成功标准

```
$ .\Features.exe
Hello, Features Project!
$
```

### ✅ 调试成功标准

1. 在 main.cpp 第 5 行设置断点
2. 按 F5 启动调试
3. 程序自动暂停在断点处
4. 左侧"变量"面板显示变量信息
5. 可使用 F10、F11 等进行单步执行

---

## 九、获得帮助

### 查看错误信息位置

| 错误来源 | 访问方式 |
|---------|----------|
| 编译错误 | Ctrl+Shift+M（问题面板） |
| CMake 错误 | 输出面板 → 选择 "CMake" |
| 调试错误 | 调试控制台（底部） |
| GDB 消息 | 调试控制台（底部） |

### 收集调试信息

```powershell
# 1. 导出编译详情
cd e:\CodeProject\Features\build
cmake --build . --verbose > build_log.txt 2>&1

# 2. 查看日志
cat build_log.txt
```

### 常用诊断命令

```powershell
# 查看编译器版本
g++ -v

# 查看编译器搜索路径
g++ -print-search-dirs

# 查看库搜索路径
ld --verbose | grep SEARCH_DIR

# 检查链接库
ldd .\Features.exe
```

---

## 十、性能优化配置

### Debug 模式（开发时使用）
```powershell
cmake -DCMAKE_BUILD_TYPE=Debug ..
# 优点：包含完整调试符号，易于调试
# 缺点：文件大，运行慢
```

### Release 模式（发布时使用）
```powershell
cmake -DCMAKE_BUILD_TYPE=Release ..
# 优点：优化代码，文件小，运行快
# 缺点：无调试符号，难以调试
```

### 编译优化选项

```cmake
# 在 CMakeLists.txt 中
set(CMAKE_CXX_FLAGS_DEBUG "-g3 -O0")      # 完全调试，无优化
set(CMAKE_CXX_FLAGS_RELEASE "-O3")        # 最大优化，无调试
```

---

**最后检查：** 所有项目都应该能通过以上检查清单，如有问题请参考对应的故障排除部分。

**最后修改：2026-02-24**
